/* This program prints the even numbers from 4 to 20 and there squares*/
#include <stdio.h>
int main(){
	/* this will use the easy solution that somewhat works*/
	int x;
	for(x = 4; x <= 20; x = x + 2){
	printf(" %d",x);
	}
	printf("\n");
return 0;
}
